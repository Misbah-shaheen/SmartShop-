name: smartshop_plus
description: SmartShop+ - A modern mini e-commerce Flutter application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.2

  # Google Fonts for beautiful typography
  google_fonts: ^6.2.1

  # Icons
  cupertino_icons: ^1.0.8

  # Firebase (optional - structure ready)
  # firebase_core: ^2.24.2
  # firebase_auth: ^4.15.3
  # cloud_firestore: ^4.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

